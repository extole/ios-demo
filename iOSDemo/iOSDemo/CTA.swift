import Foundation

public class CTA {
    var subject: String
    var message: String

    init(subject: String = "", message: String = "") {
        self.subject = subject
        self.message = message
    }
}
